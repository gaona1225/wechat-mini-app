<view class="bd">
  <view class="bd-text">设置用户名和密码后，可采用用户名/手机号+密码的形式登录百度系所有产品</view>
  <view class="bd-content">
    <view class="bd-username">
      <label>用户名</label>
      <input class="input-name" bindinput="userNameInput" placeholder="6-14位中英文、数字和下划线组合" value="{{user_name}}"/>
      <label style="display:{{clearIcon}}" bindtap="clearInput" class="input-clearValue"></label>
    </view>
    <view class="bd-password">
      <label>密码</label>
      <input bindinput="passwordInput" value="{{pass_word}}" class="input-pass" type="{{inputType}}" placeholder="6-14位数字、字母、符号组合" />
      <label  style="display:{{eyeIcon}}"  class="input-eye {{openEye?'input-eye-open':''}}" bindtap="eyeShowEvent"></label>
    </view>
  </view>
  <view>
    <button bindtap="regUserBtn" style="color:{{btnColor}}" class="submit-btn">提交</button>
  </view>
  <view>
        <text style="display:{{ignoreBtn}}" bindtap="ignoreSetinfo" class="ignore-set">跳过</text>
    </view>
  <view style="display:{{toast?'block':''}}" class="bd-toast">{{toasttxt}}</view>
</view>
